<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="useCase"
            type="jp.toastkid.yobidashi.media.image.preview.attach.MenuActionUseCase"
            />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/soft_background"
        >

        <TextView
            android:id="@+id/this_app"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="This app"
            android:textSize="16sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:onClick="@{useCase::thisApp}"
            android:background="@drawable/ripple_transparent"
            />

        <TextView
            android:id="@+id/other_app"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:padding="16dp"
            android:text="Other app"
            android:textSize="16sp"
            app:layout_constraintTop_toBottomOf="@id/this_app"
            android:onClick="@{useCase::otherApp}"
            android:background="@drawable/ripple_transparent"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>