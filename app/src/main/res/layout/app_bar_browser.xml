<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable name="fragment"
            type="jp.toastkid.yobidashi.browser.BrowserFragment" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        tools:background="@color/colorPrimaryDark"
        android:padding="4dp"
        >
        <FrameLayout
            android:id="@+id/box"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/back"
            >
            <LinearLayout
                android:id="@+id/url_box"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="36dp"
                android:background="@drawable/url_box_background"
                android:onClick="@{view -> fragment.tapHeader()}"
                >

                <TextView
                    android:id="@+id/main_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="40dp"
                    android:layout_marginEnd="40dp"
                    android:gravity="center_vertical"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:textSize="14sp"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toEndOf="@id/icon"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="Title" />

                <TextView
                    android:id="@+id/sub_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="40dp"
                    android:layout_marginEnd="40dp"
                    android:layout_marginBottom="4dp"
                    android:textSize="9sp"
                    android:gravity="center_vertical"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="https://www.yahoo.com"
                    />
            </LinearLayout>
            <ImageView
                android:id="@+id/icon"
                android:layout_width="28dp"
                android:layout_height="match_parent"
                android:padding="4dp"
                android:layout_marginStart="4dp"
                android:layout_marginEnd="4dp"
                android:contentDescription="@string/search"
                app:srcCompat="@drawable/ic_reader_mode"
                android:scaleType="centerInside"
                android:adjustViewBounds="true"
                android:background="@drawable/ripple_transparent"
                android:onClick="@{view -> fragment.openReaderMode()}"
                />

            <ImageView
                android:id="@+id/reload"
                android:layout_width="32dp"
                android:layout_height="match_parent"
                android:layout_gravity="right|end"
                app:srcCompat="@drawable/ic_reload"
                android:scaleType="centerInside"
                android:adjustViewBounds="true"
                android:background="@drawable/ripple_transparent"
                android:onClick="@{view -> fragment.reload()}"
                />
            <ProgressBar
                android:id="@+id/progress"
                style="@style/Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_gravity="bottom"
                android:visibility="gone"
                tools:visibility="visible"
                />
        </FrameLayout>

        <ImageView
            android:id="@+id/back"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:layout_marginLeft="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:srcCompat="@drawable/ic_back"
            tools:tint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.back()}"
            />
        <ImageView
            android:id="@+id/forward"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:srcCompat="@drawable/ic_forward"
            tools:tint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/back"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.forward()}"
            />
        <FrameLayout
            android:id="@+id/tab_list"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/forward"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.tabList()}"
            >
            <ImageView
                android:id="@+id/tab_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                app:srcCompat="@drawable/ic_tab"
                tools:tint="@color/white"
                android:contentDescription="@string/tab_list"
                />
            <TextView
                android:id="@+id/tab_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="2dp"
                android:layout_marginBottom="2dp"
                android:layout_gravity="center"
                tools:textColor="@color/white"
                android:textSize="10dp"
                tools:text="99"
                tools:ignore="SpUsage"
                />
        </FrameLayout>
        <ImageView
            android:id="@+id/user_agent"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:srcCompat="@drawable/ic_user_agent"
            tools:tint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/tab_list"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.showUserAgentSetting()}"
            />
        <ImageView
            android:id="@+id/page_information"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:srcCompat="@drawable/ic_info"
            tools:tint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/user_agent"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.showPageInformation()}"
            />
        <ImageView
            android:id="@+id/html_source"
            android:layout_width="48dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:srcCompat="@drawable/ic_code"
            tools:tint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/box"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/page_information"
            android:background="@drawable/ripple_transparent"
            android:onClick="@{view -> fragment.showHtmlSource()}"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>